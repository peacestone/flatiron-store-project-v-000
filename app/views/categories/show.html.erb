<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %></li>
    <%= number_to_currency(item.price.to_f/100) %>
    <%= button_to "Add to Cart", line_items_path if current_user %>
<% end %>
</ul>
